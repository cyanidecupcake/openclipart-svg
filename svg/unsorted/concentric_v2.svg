<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<svg
   xmlns:dc="http://purl.org/dc/elements/1.1/"
   xmlns:cc="http://creativecommons.org/ns#"
   xmlns:rdf="http://www.w3.org/1999/02/22-rdf-syntax-ns#"
   xmlns:svg="http://www.w3.org/2000/svg"
   xmlns="http://www.w3.org/2000/svg"
   xmlns:xlink="http://www.w3.org/1999/xlink"
   xmlns:sodipodi="http://sodipodi.sourceforge.net/DTD/sodipodi-0.dtd"
   xmlns:inkscape="http://www.inkscape.org/namespaces/inkscape"
   viewBox="-5 -5 410 410"
   preserveAspectRatio="xMidYMid"
   width="100%"
   height="100%"
   id="svg3002"
   version="1.1"
   onload="startup(evt)">
  <script>
    <![CDATA[
             var svgDocument=null;
             var T,ASG;
             var lMA = -1;
             var numCopies = 7;
             var updateTimeMillis = 100;
             var shapeArray = new Array(numCopies);
             var pathArray = new Array(numCopies);
             var sInHD = 60*60*12;
             var SVGns = "http://www.w3.org/2000/svg";
             var SVGxmlns = 'http://www.w3.org/2000/xmlns/';
             var SVGxlink = 'http://www.w3.org/1999/xlink';
             function pad2(number) {
               var str = "" + ~~number;
               while(str.length < 2){
                 str = "0" + str;
               }
               return(str);
             }
             function startup(evt){
               svgDocument=evt.target.ownerDocument;
               T = svgDocument.getElementById("tTime");
               for(i = 1; i < numCopies; i++){
                 shapeArray[i] = svgDocument.getElementById("gC"+i);
                 pathArray[i] = svgDocument.getElementById("pC"+i);
               }
               window.setInterval("changeTime()",updateTimeMillis);
               changeTime();
             }
             function changeTime(){
               var currentDate = new Date();
               var dateSeconds = (currentDate.getTime() / 1000) % (sInHD);
               var secInt = ~~dateSeconds;
               <!-- var secFrac = -->
               <!--    (1 - Math.sin((0.5 - (dateSeconds - secInt)) * Math.PI))/2; -->
               var secFrac = (dateSeconds - secInt);
               var hourPart = currentDate.getHours();
               var minPart = currentDate.getMinutes();
               var hAng = ((dateSeconds / sInHD) * 360);
               var mAng = (~~((dateSeconds % 3600) / 60) * 6);
               var sAng = (((secInt + secFrac) % 60) / 60 +
                           (minPart % 2)) * 360;
               for(i = 1; i < numCopies; i++){
                 shapeGroup = shapeArray[i];
                 pathElement = pathArray[i];
                 var loopAngle = ((60/i) * sAng) % 720;
                 var loopMod = loopAngle % 360;
                 var lA = 0; // large arc flag
                 var sF = 0; // sweep flag
                 if(loopAngle < 360){
                   sF = 1;
                 } else {
                   lA = 1;
                 }
                 if(loopMod >= 180){
                   lA = (1 - lA);
                 }
                 var lR = 30 * i; // loop radius
                 moveX = Math.cos((loopAngle+180) * (Math.PI/180)) * lR;
                 moveY = Math.sin((loopAngle+180) * (Math.PI/180)) * lR;
                 <!-- shapeGroup.setAttribute("transform", -->
                 <!--   "rotate("+(loopAngle)+" 0 0)"); -->
                 pathElement.setAttribute("d",
                 "M -"+lR+",0 A "+lR+" "+lR+" "+(-loopAngle)+" "+
                   lA+" "+sF+" "+(moveX)+","+(moveY));
               }
             }
             //]]>
  </script>
  <rect x="-5" y="-5" width="410" height="410" fill="black" />
  <g transform="translate(200 200)">
    <g id="gC0">
      <circle cx="0" cy="0" r="10" fill="white" stroke="none" />
    </g>
    <g id="gC1">
      <path fill="none" stroke="white"
            stroke-width="20" stroke-linecap="round"
            id="pC1" d="M -30,0 A 30 30 0 1 1 -30,0" />
    </g>
    <g id="gC2">
      <path fill="none" stroke="white"
            stroke-width="20" stroke-linecap="round"
            id="pC2" d="M -60,0 A 60 60 0 1 1 -60,0" />
    </g>
    <g id="gC3">
      <path fill="none" stroke="white"
            stroke-width="20" stroke-linecap="round"
            id="pC3" d="M -90,0 A 90 90 0 1 1 -90,0" />
    </g>
    <g id="gC4">
      <path fill="none" stroke="white"
            stroke-width="20" stroke-linecap="round"
            id="pC4" d="M -120,0 A 120 120 0 1 1 -120,0" />
    </g>
    <g id="gC5">
      <path fill="none" stroke="white"
            stroke-width="20" stroke-linecap="round"
            id="pC5" d="M -150,0 A 150 150 0 1 1 -150,0" />
    </g>
    <g id="gC6">
      <path fill="none" stroke="white"
            stroke-width="20" stroke-linecap="round"
            id="pC6" d="M -180,0 A 180 180 0 1 1 -180,0" />
    </g>
  </g>
</svg>
